"use client";import {useEffect,useState} from "react";export default function Admin(){const [d,setD]=useState<any>();useEffect(()=>{fetch("/api/admin").then(r=>r.json()).then(setD)},[]);if(!d)return <div className="panel">Admin only / loading…</div>;return <><h1>Admin Dashboard</h1><div className="stats">{[[d.users,"Users"],[d.projects,"Projects"],[d.transactions,"Transactions"],[d.downloads,"Downloads"],[d.views,"Views"],[`$${(d.revenue/100).toFixed(2)}`,"Revenue"]].map(([n,l])=><div className="stat" key={l}><b>{n}</b>{l}</div>)}</div><div className="panel"><h2>Moderation controls</h2><p>Project status, featured flag, coupons, reports, users, transactions, audit/analytics models are included in the data layer. Add operational admin CRUD as needed for your deployment policy.</p></div></>}
