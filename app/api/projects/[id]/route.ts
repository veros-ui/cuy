import { NextResponse } from "next/server";
import { prisma } from "@/lib/prisma";
import { requireUser } from "@/lib/session";
export async function GET(_:Request,{params}:{params:{id:string}}){const p=await prisma.project.findUnique({where:{id:params.id},include:{owner:{select:{id:true,name:true,email:true,image:true,bio:true,role:true}},reviews:{include:{user:{select:{name:true,image:true}},},orderBy:{createdAt:"desc"}}}});if(!p)return NextResponse.json({error:"Not found"},{status:404});await prisma.project.update({where:{id:p.id},data:{views:{increment:1}}});return NextResponse.json(p)}
export async function PATCH(req:Request,{params}:{params:{id:string}}){try{const u=await requireUser();const p=await prisma.project.findUnique({where:{id:params.id}});if(!p||p.ownerId!==u.id&&u.role!=="ADMIN")return NextResponse.json({error:"Forbidden"},{status:403});const d=await req.json();return NextResponse.json(await prisma.project.update({where:{id:p.id},data:{...d,price:d.price===undefined?undefined:Number(d.price)}}));}catch{return NextResponse.json({error:"Forbidden"},{status:403})}}
export async function DELETE(_:Request,{params}:{params:{id:string}}){try{const u=await requireUser();const p=await prisma.project.findUnique({where:{id:params.id}});if(!p||p.ownerId!==u.id&&u.role!=="ADMIN")return NextResponse.json({error:"Forbidden"},{status:403});await prisma.project.delete({where:{id:p.id}});return NextResponse.json({ok:true});}catch{return NextResponse.json({error:"Forbidden"},{status:403})}}
