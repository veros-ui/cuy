import { NextResponse } from "next/server";import { prisma } from "@/lib/prisma";import { requireUser } from "@/lib/session";
export async function POST(_:Request,{params}:{params:{id:string}}){try{const u=await requireUser();const existing=await prisma.wishlist.findUnique({where:{userId_projectId:{userId:u.id,projectId:params.id}}});if(existing){await prisma.wishlist.delete({where:{id:existing.id}});return NextResponse.json({saved:false})}await prisma.wishlist.create({data:{userId:u.id,projectId:params.id}});return NextResponse.json({saved:true});}catch{return NextResponse.json({error:"Login required"},{status:401})}}
