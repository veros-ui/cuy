"use client";import {signIn} from "next-auth/react";import {useState} from "react";import Link from "next/link";import {useRouter} from "next/navigation";
export default function Login(){const [email,setEmail]=useState(""),[password,setPassword]=useState(""),[err,setErr]=useState("");const r=useRouter();async function go(e:any){e.preventDefault();const x=await signIn("credentials",{email,password,redirect:false});if(x?.error)setErr("Email atau password salah.");else r.push("/dashboard")}return <div className="panel form"><h1>Login</h1><input type="email" placeholder="Email" value={email} onChange={e=>setEmail(e.target.value)}/><input type="password" placeholder="Password" value={password} onChange={e=>setPassword(e.target.value)}/><button onClick={go}>Login</button><button onClick={()=>signIn("google",{callbackUrl:"/dashboard"})}>Continue with Google</button>{err&&<p className="danger">{err}</p>}<p className="muted">Belum punya akun? <Link href="/register">Register</Link></p></div>}
