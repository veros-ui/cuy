"use client";
import {useEffect,useState} from "react";
import {useSession,signOut} from "next-auth/react";

export default function Home(){const{s}= {s:useSession()};const[data,setData]=useState<any[]>([]);useEffect(()=>{fetch('/api/projects').then(x=>x.json()).then(setData)},[]);return <main><header><a className="brand" href="/">KINGDAPA<span>-HIZ</span></a><nav><a href="#projects">Projects</a>{s?.data?.user?.role==='ADMIN'&&<a href="/admin">Admin</a>}<button className="ghost" onClick={()=>s?.data?signOut():location.href='/login'}>{s?.data?'Keluar':'Masuk'}</button></nav></header><section className="hero"><div><p className="eyebrow">DIGITAL PROJECT MARKETPLACE</p><h1>Project bagus.<br/><i>Langsung pakai.</i></h1><p className="lead">Koleksi project siap pakai untuk developer. Pilih yang gratis atau buka project premium.</p><a className="primary cta" href="#projects">Jelajahi project</a></div><div className="hero-grid"><span/><span/><span/><span/></div></section><section id="projects" className="projects"><div className="section-head"><div><p className="eyebrow">COLLECTION</p><h2>Project terbaru</h2></div><span>{data.length} project</span></div><div className="grid">{data.map(p=><article className="card" key={p.id}><div className="cover">{p.photoUrl?<img src={p.photoUrl} alt=""/>:<div className="cover-placeholder">KH</div>}<b className={p.premium?'premium':'free'}>{p.premium?'PREMIUM':'FREE'}</b></div><div className="card-body"><div className="tag">{p.category}</div><h3>{p.name}</h3><p>{p.description}</p><div className="card-foot">{p.premium?<strong>Rp {p.price.toLocaleString('id-ID')}</strong>:<strong>Gratis</strong>}<a href={p.premium?`/checkout/${p.id}`:p.downloadUrl} target={p.premium?'_self':'_blank'}>{p.premium?'Beli project':'Download'} <span>→</span></a></div></div></article>)}</div></section></main>}
