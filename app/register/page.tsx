"use client";import{useState}from"react";import{signIn}from"next-auth/react";import{useRouter}from"next/navigation";
export default function Register(){const[n,setN]=useState("");const[e,setE]=useState("");const[p,setP]=useState("");const[err,setErr]=useState("");const r=useRouter();async function go(x:any){x.preventDefault();const q=await fetch("/api/register",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({username:n,email:e,password:p})});const d=await q.json();if(!q.ok)return setErr(d.error);await signIn("credentials",{email:e,password:p,redirect:false});r.push("/")}return <main className="auth"><div className="orb b"/><div className="auth-card"><div className="brand">PROJECT<span>VAULT</span></div><p className="eyebrow">CREATE ACCOUNT</p><h1>Buat akun baru</h1><p className="muted">Satu akun untuk seluruh koleksi project.</p><form onSubmit={go}><label>Username<input value={n} onChange={x=>setN(x.target.value)} required/></label><label>Email<input type="email" value={e} onChange={x=>setE(x.target.value)} required/></label><label>Password<input type="password" minLength={6} value={p} onChange={x=>setP(x.target.value)} required/></label>{err&&<p className="error">{err}</p>}<button className="primary">Buat akun</button></form><p className="switch">Sudah punya akun? <a href="/login">Masuk</a></p></div></main>}