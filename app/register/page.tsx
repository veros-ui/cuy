"use client";import {useState} from "react";import {useRouter} from "next/navigation";import Link from "next/link";
export default function Register(){const [v,setV]=useState({username:"",email:"",password:""}),[err,setErr]=useState("");const r=useRouter();async function go(e:any){e.preventDefault();const x=await fetch("/api/register",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(v)});const d=await x.json();if(!x.ok){setErr(d.error);return}r.push("/login")}return <div className="panel form"><h1>Create account</h1><input placeholder="Username" value={v.username} onChange={e=>setV({...v,username:e.target.value})}/><input type="email" placeholder="Email" value={v.email} onChange={e=>setV({...v,email:e.target.value})}/><input type="password" placeholder="Password (min 8)" value={v.password} onChange={e=>setV({...v,password:e.target.value})}/><button onClick={go}>Register</button>{err&&<p className="danger">{err}</p>}<p className="muted">Sudah punya akun? <Link href="/login">Login</Link></p></div>}
