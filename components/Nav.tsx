"use client";
import Link from "next/link";import {useSession,signOut} from "next-auth/react";import {useEffect,useState} from "react";
export default function Nav(){const {data}=useSession();const [unread,setUnread]=useState(0);useEffect(()=>{if(!data?.user){setUnread(0);return}let live=true;const load=async()=>{const r=await fetch("/api/chats/unread");const x=await r.json();if(live)setUnread(Number(x.count||0))};load();const t=setInterval(load,15000);return()=>{live=false;clearInterval(t)}},[data?.user]);const chatLabel=<span>Chat Admin{unread>0&&<span className="chatbadge">{unread>99?"99+":unread}</span>}</span>;return <header><Link href="/" className="brand">ProjectVault</Link><details className="hamburger"><summary>☰</summary><div className="menu">{data?.user?<><Link href="/">Marketplace</Link><Link href="/dashboard">Dashboard</Link><Link href="/wishlist">Wishlist</Link><Link href="/chat">{chatLabel}</Link><Link href="/premium">Premium</Link><Link href="/profile">Profile</Link>{data.user.role==="ADMIN"&&<Link href="/admin">Admin</Link>}<button onClick={()=>signOut({callbackUrl:"/"})}>Logout</button></>:<><Link href="/">Marketplace</Link><Link href="/login">Login</Link><Link href="/register">Register</Link></>}</div></details><nav className="desktopnav"><Link href="/">Marketplace</Link>{data?.user?<><Link href="/dashboard">Dashboard</Link><Link href="/wishlist">Wishlist</Link><Link href="/chat">{chatLabel}</Link><Link href="/premium">Premium</Link><Link href="/profile">Profile</Link>{data.user.role==="ADMIN"&&<Link href="/admin">Admin</Link>}<button onClick={()=>signOut({callbackUrl:"/"})}>Logout</button></>:<><Link href="/login">Login</Link><Link className="btn" href="/register">Register</Link></>}</nav></header>}
